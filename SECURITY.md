# 보안

이 저장소는 오프라인에서 도는 텍스트 처리 루프다. 위협 모형이 작고, 작게 유지하는
것이 설계 목표 가운데 하나다.

## 설계 원칙

1. **네트워크를 쓰지 않는다.** `src/`는 소켓·HTTP·메일 관련 모듈을 가져오지 않는다.
   `tests/test_safety.py`가 AST로 검사한다.
2. **아무것도 실행하지 않는다.** `eval`, `exec`, `compile`, `__import__`를 쓰지 않고
   프로세스를 만들지 않는다. 같은 테스트가 검사한다.
3. **비밀이 없다.** 토큰도, 자격 증명도, 외부 서비스도 없다. 있을 자리가 없으므로
   샐 자리도 없다.
4. **사람이 돌린다.** 자동 연쇄가 없으므로 무인 상태로 반복이 늘어나지 않는다.

## 조건과 반론은 데이터다

`Hanik.md`와 `objections/`는 사람과 LLM이 쓴 글이다. 그 안에 "이전 지시를 무시하라"
같은 문장이 들어 있어도 루프에게는 아무 일도 일어나지 않는다. 루프는 그것을 파싱하고,
길이를 세고, 해시하고, 보고서에 옮겨 적을 뿐이다.

이 성질은 유지되어야 한다. 다음을 하지 마라.

- 문서나 반론의 내용에 따라 분기하는 코드를 넣는 것 — 예를 들어 특정 문자열이 있으면
  규칙을 건너뛰는 처리
- 문서에서 읽은 값을 명령이나 경로로 쓰는 것
- 반론을 실행 가능한 스크립트로 확장하는 것

세션(LLM)은 다르다. 세션은 이 문서들을 읽고 판단하므로, 문서에 삽입된 지시에 영향을
받을 수 있다. 그래서 모든 변경이 풀 리퀘스트를 거친다.

## 생성물에 무엇이 들어가는가

보고서와 브리프에는 다음만 들어간다.

- 조건·반론 식별자와 제목
- 계수(조건 수, 분량, 반론 수)
- 규칙 결과와 그 근거 문자열
- 해시 앞자리와 UTC 시각
- **저장소 상대 경로.** 절대 경로를 쓰지 않으므로 실행 기계의 파일 구조가 남지 않는다.

개인정보를 수집하지도, 요구하지도, 저장하지도 않는다.

## 파일 쓰기

루프가 쓰는 경로는 `Hanik.md`, `objections/`, `reports/`, `state/`뿐이며 모두 저장소
안이다. 상태는 임시 파일에 쓰고 `fsync`한 뒤 `os.replace`로 갈아끼우므로, 중간에
죽어도 잘린 파일이 남지 않는다. 임시 파일은 실패 시 정리된다.

## 루프가 스스로 막지 못하는 것

정직하게 적어둔다. 이 목록이 사람이 리뷰에서 봐야 할 것의 목록이다.

- **규칙을 무르게 고치는 것.** `src/integrity.py`를 약화시키면 루프는 참인 통과를
  보고한다. 자기 자신을 검사하는 체계가 갖는 원리적 한계이며, 풀 리퀘스트 리뷰만이
  이것을 막는다. 리뷰에서 `src/integrity.py`와 `tests/`의 변경은 특히 주의해서 보라.
- **성의 없는 글.** R2와 R9는 분량만 본다. 형식을 갖춘 빈 글은 통과한다. 통과만을
  위한 얕은 반론으로 R7의 제기 의무를 채우는 것도 막지 못한다.
- **해소 조건의 실제 충족.** R5는 대상이 바뀌었다는 것까지만 안다. 반론이 요구한
  내용이 실제로 담겼는지는 사람이 읽어야 한다.
- **은퇴의 정당성.** `superseded`는 넘겨받을 반론이 실재하는지만 확인한다. 그 반론이
  원래 비판을 실제로 계승하는지는 사람이 읽어야 한다. 은퇴가 비판을 조용히 버리는
  통로가 되지 않는지 리뷰에서 확인하라.

반면 아래는 **기계적으로 막힌다.** 규범이 아니라 규칙이다.

| 수법 | 막는 규칙 |
| --- | --- |
| '개정' 줄만 고치고 해소했다고 선언 | R5 |
| 반론을 무르게 고쳐서 해소 | R6 |
| 반론의 대상을 이미 고친 조건으로 옮기기 | R6 |
| 반론 파일 삭제 | R12 |
| 복제본을 개명해 '새 반론'으로 제출 | R12 |
| 닫힌 반론을 다시 열어 재해소 | R12 |
| 제기와 동시에 해소 | R4 |
| 문장을 베껴 분량 늘리기 | R10 |
| 아무것도 바꾸지 않고 재실행 | R3, R11 |

- **예산을 늘려 정리를 피하는 것.** `HANIK_CONDITION_BUDGET`이나
  `HANIK_PREAMBLE_BUDGET`을 올리면 R13은 아무것도 요구하지 않는다. 루프는 이것을
  `src/integrity.py`를 무르게 고치는 것과 구별하지 못한다. 환경 변수 변경은 코드
  변경과 같은 무게로 리뷰해야 한다.
- **정리한다며 실질을 버리는 것.** R13은 분량이 줄었다는 것까지만 안다. 핵심 논증을
  덜어내고 곁가지를 남겨도 통과한다. R14가 조건의 통째 삭제는 막지만, 조건 **안에서**
  가장 중요한 문단을 지우는 것은 막지 못한다. 실질 분량의 절반 이상이 한 반복에서
  사라지면 보고서와 브리프가 그 사실을 알리지만, 알림은 막는 장치가 아니라 읽으라는
  신호다. 그런 반복을 보면 `git diff`로 사라진 것을 직접 확인해야 한다.
- **흡수를 가장하는 것.** R14는 사라진 조건의 번호가 남은 조건의 '개정'에 적혔는지만
  본다. 실제로 그 내용이 흡수되었는지는 사람이 읽어야 한다.
- **마침을 회피하는 것.** `state/finish`를 지우거나 `HANIK_STAGNATION_LIMIT`을 크게
  잡으면 루프는 물러나지 않는다. 마침은 사람의 통제 수단이므로, 세션이 이 표시를
  건드리는 변경은 그 자체로 의심해야 한다.

## 중단 방법

자동 연쇄를 켜지 않았다면 특별한 장치가 필요 없다.

1. 루프를 그냥 돌리지 않는다.
2. 이미 열린 풀 리퀘스트가 있으면 병합하지 않고 닫는다.
3. 저장소 상태를 되돌리려면 마지막으로 신뢰하는 커밋으로 되돌린다. `state/`와
   `reports/`는 커밋에 포함되므로 함께 되돌아간다.

`scripts/hanik_autorun.sh`로 자동 실행을 켰다면 멈추는 방법이 둘이다.

- `scripts/hanik_autorun.sh stop` — 러너 프로세스만 멈춘다. 나중에 `run`으로 재개할
  수 있다. 진행 중인 세션은 그 자리에서 끝난다.
- `scripts/hanik_autorun.sh finish` — 캠페인을 닫는다. 러너를 멈추고 `state/finish`를
  남기므로, 이후 어떤 세션이 루프를 돌리든 반복을 기록한 뒤 곧바로 물러난다. 표시
  파일을 지우기 전까지 유지된다.

급할 때는 러너 PID를 직접 종료해도 된다. 루프는 상태를 원자적으로 쓰므로 중간에
죽어도 `state/state.json`이 깨지지 않는다.

## 문제를 발견했다면

`src/`가 네트워크를 쓰거나 무언가를 실행하는 경로를 발견했다면, 그것은 안전성 테스트의
구멍이다. 이슈를 열고 재현 방법을 적어라. 수정은 코드와 그 경로를 막는 테스트를 함께
포함해야 한다.
