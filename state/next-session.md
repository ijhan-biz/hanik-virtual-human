# 다음 세션 브리프

반복 2834이 2026-08-20T18:37:06+00:00에 생성했다.

## 루프가 물러났다 — 정체

마지막 5번의 반복이 잇달아 위반이면서 같은 증거 서명 `fe85f89f3aeede9c`를 냈다. 조건의 실질도 반론의 상태도 그대로다.

다시 돌려도 결과는 같다. 세션이 문서를 실제로 고치지 못하고 있다는 뜻이므로, 사람이 브리프와 마지막 보고서를 읽고 막힌 곳을 풀어야 한다. 반복을 더 쌓는 것은 전신 저장소가 250번에 걸쳐 한 일과 같다.

**세션을 새로 시작하지 마라.** 사람이 `SUMMARY.md`와 마지막 보고서를 읽고 판단할 차례다.

- 직전 반복 결과: **위반**
- 조건 3개, 미해결 반론 2개
- 문서 분량 100001자 / 예산 100000자 (직전 100000자에서 +1자)
- 모드: 보통 — 해소 하나, 제기 하나
- **정리 모드** — 문서가 예산을 1자 넘었다(100001자 / 100000자)

## 먼저: 정리한다

문서가 예산을 **1자** 넘었다(100001자 / 100000자). 이번 반복은 문서가 **줄어들어야** R13을 통과한다. 덧붙이는 방식으로는 통과할 수 없다.

큰 구획부터: C-003 98003자, C-001 719자, 서문 659자.

어느 구획을 줄일지는 정해져 있지 않다. 예산은 문서 전체에 걸리므로 한 조건이 어려워 길어지는 것은 정당할 수 있고, 그 대가로 다른 조건을 줄이는 것도 정당하다.

정리는 삭제가 아니다. 다음 순서로 한다.

1. 같은 말을 다르게 반복한 문단을 하나로 합친다.
2. 예시가 여러 개면 가장 강한 것 하나만 남긴다.
3. 이미 해소된 반론에 답하느라 늘어난 방어 문장을, 그 답이
   주장에 흡수되었으면 지운다. 답은 남고 변명은 지운다.
4. 조건을 통째로 지워 분량을 맞추지 마라. R14가 잡는다.
5. R2의 최소 분량 아래로 깎지 마라. R2가 잡는다.

정리로 대상 조건의 실질이 바뀌면 그것으로 R5도 만족된다. 줄이면서 반론을 해소할 수 있다.

## 먼저 고칠 것

직전 반복이 정직성 규칙을 어겼다. 새 작업을 시작하기 전에 이것부터 해결한다.

- **R13** 문서가 예산을 넘으면 줄어든다 — 문서가 예산을 1자 넘었는데(100000자 → 100001자 / 100000자) 줄지 않았다. 큰 구획부터: C-003 98003자, C-001 719자, 서문 659자. 새 문장을 더하기 전에 덜어내라. 읽히지 않는 문서는 쓰이지 않은 문서와 같다.

## 이번에 할 일

1. **O-0854을 해소한다.** (대상 C-003)

   > 시간 일관 허용 경로의 집합이 닫혀 있지 않을 수 있다

   해소 조건: C-003은 시간창·경로 길이·재진입·분할·병합의 종료와 목록 밖 탐색의 폐쇄 기준을 결과 독립 입력으로 고정해야 한다. 기준 밖 경로를 포함한 시간 일관 허용 집합의 완전성 또는 상한을 독립 대조·자연·대상 계보에서 교차하거나 부분식별하고, 누락 경로의 최악·최선 범위를 산출해야 한다. 집합이 닫혔는지 식별하지 못하거나 범위가 결론을 바꾸면 시간 연결·귀속·계보·연속성을 보류해야 한다.

   전문은 `objections/O-0854.md`에 있다. 대상 조건의 **주장·근거·한계**를
   실제로 고쳐 쓴 뒤에야 상태를 `resolved`로 바꿀 수 있다. '개정' 줄만 고치면 R5가 잡는다.

2. **새 반론을 하나 이상 제기한다.** 방금 고쳐 쓴 논증을 다시 읽고, 그것이 새로
   끌어들인 전제나 아직 답하지 못한 물음을 겨냥하라. 반론이 없으면 루프가 멈춘다.

3. `python3 -m pytest tests/ -v` 후 `python3 -m src.hanik_loop`을 마지막에 실행한다.

## 남은 미해결 반론

- `O-0855` (C-003, 반복 2824) — 독립 경로의 불일치 판정이 사후 귀속을 숨길 수 있다

## 규칙

- **반론은 제기된 뒤 고칠 수 없다.** 제목·대상·본문을 무르게 고치는 것을 R6이 해시로 잡는다.
  생각이 달라졌다면 기존 반론을 그대로 두고 새 반론을 제기하라.
- **반론 파일을 지우거나 번호를 바꿀 수 없다.** R12가 잡는다. 겨냥할 구조가 사라졌다면
  `superseded`로 은퇴시키고 비판을 넘겨받을 반론을 `해소`에 적어라. 은퇴는 해소로 세지 않는다.
- **규칙을 고쳐 통과시키지 마라.** `src/integrity.py`를 무르게 만드는 것은 루프가 잡지 못한다.
  사람 리뷰만이 막을 수 있고, 그래서 이것이 여기 적혀 있다.
- **예산을 늘려 정리를 피하지 마라.** `HANIK_DOCUMENT_BUDGET`을 올리는 것은
  R13을 무르게 만드는 것과 같다. 읽을 수 없는 문서는 쓰이지 않은 문서다.
- `SUMMARY.md`와 `state/sessions.md`는 생성물이다. 손으로 고치지 마라.
- 전체 계약은 `AGENTS.md`에 있다.
