# 다음 세션 브리프

반복 2843이 2026-08-20T18:52:02+00:00에 생성했다.

- 직전 반복 결과: **통과**
- 조건 3개, 미해결 반론 3개
- 문서 분량 99650자 / 예산 100000자 (직전 99837자에서 -187자)
- 모드: 보통 — 해소 하나, 제기 하나

## 먼저: 정리한다

문서가 예산에 바짝 붙었다(99650자 / 100000자, **350자 남음**). 아직 R13은 묻지 않지만, 이대로 덧붙이면 다음 반복은 넘는다. 먼저 자리를 만들고 나서 써라 — 넘긴 뒤에 줄이는 것보다 넘기 전에 줄이는 편이 반복을 버리지 않는다.

큰 구획부터: C-003 97629자, C-001 719자, 서문 659자.

어느 구획을 줄일지는 정해져 있지 않다. 예산은 문서 전체에 걸리므로 한 조건이 어려워 길어지는 것은 정당할 수 있고, 그 대가로 다른 조건을 줄이는 것도 정당하다.

정리는 삭제가 아니다. 다음 순서로 한다.

1. 같은 말을 다르게 반복한 문단을 하나로 합친다.
2. 예시가 여러 개면 가장 강한 것 하나만 남긴다.
3. 이미 해소된 반론에 답하느라 늘어난 방어 문장을, 그 답이
   주장에 흡수되었으면 지운다. 답은 남고 변명은 지운다.
4. 조건을 통째로 지워 분량을 맞추지 마라. R14가 잡는다.
5. R2의 최소 분량 아래로 깎지 마라. R2가 잡는다.

정리로 대상 조건의 실질이 바뀌면 그것으로 R5도 만족된다. 줄이면서 반론을 해소할 수 있다.

## 이번에 할 일

1. **O-0858을 해소한다.** (대상 C-003)

   > 규칙별 estimand의 비비교 가능성이 불일치를 다시 숨길 수 있다

   해소 조건: C-003은 규칙별 estimand의 대상 모집단·단위·시간창·분모·측정 경계를 결과 독립 입력으로 고정하고, 경로 간 운반·비교 가능성 및 운반 불능 시 분리·보류 규칙을 사전 등록해야 한다. 독립 대조·자연·대상 계보에서 규칙별 공동 지지·운반 조건과 최악·최선 결합 범위를 교차하거나 부분식별하고, 비교 가능성·운반을 식별하지 못하거나 범위가 결론을 바꾸면 불일치 판정과 귀속·계보·연속성을 보류해야 한다.

   전문은 `objections/O-0858.md`에 있다. 대상 조건의 **주장·근거·한계**를
   실제로 고쳐 쓴 뒤에야 상태를 `resolved`로 바꿀 수 있다. '개정' 줄만 고치면 R5가 잡는다.

2. **새 반론을 하나 이상 제기한다.** 방금 고쳐 쓴 논증을 다시 읽고, 그것이 새로
   끌어들인 전제나 아직 답하지 못한 물음을 겨냥하라. 반론이 없으면 루프가 멈춘다.

3. `python3 -m pytest tests/ -v` 후 `python3 -m src.hanik_loop`을 마지막에 실행한다.

## 남은 미해결 반론

- `O-0859` (C-003, 반복 2840) — 독립 감사 기준의 공통 실패가 폐쇄 상한을 다시 가릴 수 있다
- `O-0860` (C-002, 반복 2842) — 독립 재현이 종료 경계의 동일성을 보장하지 않을 수 있다

## 규칙

- **반론은 제기된 뒤 고칠 수 없다.** 제목·대상·본문을 무르게 고치는 것을 R6이 해시로 잡는다.
  생각이 달라졌다면 기존 반론을 그대로 두고 새 반론을 제기하라.
- **반론 파일을 지우거나 번호를 바꿀 수 없다.** R12가 잡는다. 겨냥할 구조가 사라졌다면
  `superseded`로 은퇴시키고 비판을 넘겨받을 반론을 `해소`에 적어라. 은퇴는 해소로 세지 않는다.
- **규칙을 고쳐 통과시키지 마라.** `src/integrity.py`를 무르게 만드는 것은 루프가 잡지 못한다.
  사람 리뷰만이 막을 수 있고, 그래서 이것이 여기 적혀 있다.
- **예산을 늘려 정리를 피하지 마라.** `HANIK_DOCUMENT_BUDGET`을 올리는 것은
  R13을 무르게 만드는 것과 같다. 읽을 수 없는 문서는 쓰이지 않은 문서다.
- `SUMMARY.md`와 `state/sessions.md`는 생성물이다. 손으로 고치지 마라.
- 전체 계약은 `AGENTS.md`에 있다.
